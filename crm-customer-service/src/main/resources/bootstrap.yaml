spring:
  application:
    name: crm-customer-service
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        file-extension: yaml
        namespace: public
        group: DEFAULT_GROUP
  config:
    import: nacos:${spring.application.name}
server:
  port: 8081